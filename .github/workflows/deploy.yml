name: Deploy Portfolio to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js 🔧
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: portfolio/package-lock.json

      - name: Install Dependencies 📦
        run: |
          cd portfolio
          npm install

      - name: Build Site 🚀
        run: |
          cd portfolio
          npm run build
          mkdir -p dist/stomp-landing-page
          cp -r ../stomp-landing-page/* dist/stomp-landing-page/

      - name: Deploy to GitHub Pages 🍒
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: portfolio/dist
          branch: gh-pages
          clean: true
